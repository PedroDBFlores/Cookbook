{
  api {
    testing = false
    port = 9000
    port = ${?API_PORT}
  }
  database {
    jdbcUrl = "jdbc:mysql://localhost:40000/cookbook?user=root&password=am!28--zQ"
    jdbcUrl = ${?DATABASE_URL}
  }
}
