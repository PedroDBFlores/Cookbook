{
  api {
    testing = false
    port = 9000
    port = ${?COOKBOOK_API_PORT}
  }
  jwt {
    domain = "http://localhost"
    domain = ${?COOKBOOK_JWT_DOMAIN}
    audience = "cookbook-jwt"
    domain = ${?COOKBOOK_JWT_AUDIENCE}
    realm = "cookbook"
    domain = ${?COOKBOOK_JWT_REALM}
    secret = "48!mak$ap1mRtcv7p@8dg32u7&r$%0qz"
    domain = ${?COOKBOOK_JWT_SECRET}
  }
  database {
    driver: "com.mysql.jdbc.Driver"
    jdbcUrl: "jdbc:mysql://cookbook-database:3306/cookbook"
    domain = ${?COOKBOOK_DATABASE_JDBCURL}
    username: "root"
    username = ${?COOKBOOK_DATABASE_JDBCURL}
    password: "am!28--zQ"
    password = ${?COOKBOOK_DATABASE_JDBCURL}
  }
}
