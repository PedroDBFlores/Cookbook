{
  api {
    testing = false
    port = 9000
    port = ${?API_PORT}
  }
  database {
    jdbcUrl = "jdbc:postgresql://localhost:40000/cookbook?user=postgres&password=am!28--zQ"
    jdbcUrl = ${?DATABASE_URL}
  }
}
