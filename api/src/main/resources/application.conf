{
  api {
    testing = false
    port = 9000
    port = ${?COOKBOOK_API_PORT}
  }
  database {
    driver: "com.mysql.jdbc.Driver"
    jdbcUrl: "jdbc:mysql://localhost:40000/cookbook"
    domain = ${?COOKBOOK_DATABASE_JDBCURL}
    username: "root"
    username = ${?COOKBOOK_DATABASE_USERNAME}
    password: "am!28--zQ"
    password = ${?COOKBOOK_DATABASE_PASSWORD}
  }
}
